import React from 'react';
import axios from 'axios';

function SampleData() {
  // axios.get('https://www.flickr.com/services/soap/')
  //   .then((response) => console.log(response.data));

  return (
    <div>SampleData</div>
  );
}

export default SampleData;
